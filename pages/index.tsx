import { Button } from 'antd';
import 'styles/global.scss';

const Index = () => {
  return (
    <div>
      Testing Ant Design SCSS....
      <Button size="large" type="primary" htmlType="submit">
        OK
      </Button>
    </div>
  );
};

export default Index;
